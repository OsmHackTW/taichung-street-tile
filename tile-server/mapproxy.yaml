services:
  wmts:

layers:
  - name: street 
    title: Street
    sources: [mnc]

caches:
  mnc:
    grids: [GLOBAL_WEBMERCATOR]
    sources: [mn]

sources:
  mn:
    type: mapnik
    mapfile: /mapnik.xml
    transparent: true

